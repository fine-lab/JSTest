rootStore.designerScripts = {
  btnEditonClick: function (rootStore, alias, event, rowStore) {
    // 编辑——单击事件rootStore.getStoreByAlias('bizflowId')
  },
  btnAddonClick: function (rootStore, alias, event, rowStore) {
    // 新增——单击事件const res = str.replace("被替换的字符串", "新字符串")
  }
}; // disabled-row-code [designerScripts-end]